import {router} from 'expo-router'; import {Ionicons} from '@expo/vector-icons'; import {StyleSheet,Text,View} from 'react-native'; import {Screen} from '@/components/Screen'; import {PatoAvatar} from '@/components/PatoAvatar'; import {AnimatedCard} from '@/components/AnimatedCard'; import {colors} from '@/constants/theme'; const items=[['Reglas del agente','settings','/permissions'],['Métodos de pago','card',null],['Notificaciones','notifications',null],['Ayuda','help-circle',null]] as const; export default function Profile(){return <Screen><View style={s.hero}><PatoAvatar size={100}/><Text style={s.name}>Negro</Text><Text style={s.status}>● Tu agente está activo</Text></View><View style={{gap:10}}>{items.map(([label,icon,route])=><AnimatedCard key={label} style={s.item} onPress={()=>route&&router.push(route as any)}><Ionicons name={icon as any} size={20} color={colors.yellow}/><Text style={s.itemText}>{label}</Text><Ionicons name="chevron-forward" size={18} color={colors.muted}/></AnimatedCard>)}</View></Screen>} const s=StyleSheet.create({hero:{alignItems:'center',marginBottom:26},name:{color:colors.text,fontSize:26,fontWeight:'900',marginTop:12},status:{color:colors.success,marginTop:6},item:{padding:16,flexDirection:'row',alignItems:'center',gap:12},itemText:{color:colors.text,fontWeight:'700',flex:1}});