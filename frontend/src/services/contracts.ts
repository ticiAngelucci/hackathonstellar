import {Group,PaymentPolicy,ServiceSubscription,Transaction} from '@/types'; export interface PaymentService{getTransactions():Promise<Transaction[]>;approvePayment(id:string):Promise<{txHash:string}>} export interface GroupService{getGroups():Promise<Group[]>} export interface SettingsService{getPolicy():Promise<PaymentPolicy>;updatePolicy(p:PaymentPolicy):Promise<PaymentPolicy>;getServices():Promise<ServiceSubscription[]>}