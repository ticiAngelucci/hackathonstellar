import {PropsWithChildren} from 'react'; import {Pressable,StyleSheet,ViewStyle} from 'react-native'; import Animated,{useAnimatedStyle,useSharedValue,withSpring} from 'react-native-reanimated'; import {colors,radius} from '@/constants/theme'; const A=Animated.createAnimatedComponent(Pressable); export function AnimatedCard({children,onPress,style}:PropsWithChildren<{onPress?:()=>void;style?:ViewStyle}>){const scale=useSharedValue(1); const as=useAnimatedStyle(()=>({transform:[{scale:scale.value}]})); return <A onPress={onPress} onPressIn={()=>scale.value=withSpring(.98)} onPressOut={()=>scale.value=withSpring(1)} style={[s.card,as,style]}>{children}</A>} const s=StyleSheet.create({card:{backgroundColor:colors.surface,borderRadius:radius.md,borderWidth:1,borderColor:colors.border}});